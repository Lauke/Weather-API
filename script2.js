/*       container = document.getElementById('test');
      for(let i = 0; i < response.data.list.length; i += 8){
      container.innerHTML+='<div class="items"></div>';  
  } */
  
    /*   const formattedDate = moment(response.data.list[0].dt_txt);
      displayDay.innerHTML = (formattedDate.format('dddd'));
      displayTemp.innerHTML = ` ${Math.round(response.data.list[0].main.temp)}°C`
      displayDescription.innerHTML = (response.data.list[0].weather[0].description);
      displayMinMax.innerHTML = ` ${Math.round(response.data.list[0].main.temp_min)}°C / ${Math.round(response.data.list[0].main.temp_max)}°C `;
      
    }) */


     /*  // let displayIcon = document.querySelector('.display-icon')
  let displayDay = document.getElementById('display-day');
  let displayTemp = document.getElementById('display-temp');
  let displayDescription = document.getElementById('display-description');
  let displayMinMax = document.getElementById('display-minmax');
 */